name: easy_stripe_payment
description: "A new Flutter package project."
version: 1.0.0
homepage: https://github.com/Just4prog/easy_stripe_payment

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_stripe: any
  dio: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

